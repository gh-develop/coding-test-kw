#include<bits/stdc++.h>
using namespace std;

int main(void){
	ios::sync_with_stdio(0);
	cin.tie(0);
	
	string s;
	while(getline(cin, s)){
		if(s[0] == '.') break;
		cout << s << '\n';
	}
		
	
	return 0;
}
